# prometheus-dnsmasq-exporter 构建配方 (Phase 1 自动生成)
repo: google/dnsmasq_exporter
package: prometheus-dnsmasq-exporter
summary: "Prometheus exporter for dnsmasq"
description: |
  Prometheus exporter for dnsmasq
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/google/dnsmasq_exporter
section: net

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "v0.3.0"
latest_tag: "v0.3.0"
commit_hash: "b76e607a31b2c6d699e1e82062834c7420dbbc00"
upstream_version: "0.3.0"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
  - loong64
  - riscv64

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []
