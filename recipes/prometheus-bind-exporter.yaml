# prometheus-bind-exporter 构建配方 (Phase 1 自动生成)
repo: prometheus-community/bind_exporter
package: prometheus-bind-exporter
summary: "Prometheus exporter for BIND server metrics"
description: |
  Prometheus exporter for BIND server metrics
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/prometheus-community/bind_exporter/
section: net

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "v0.8.0"
latest_tag: "v0.8.0"
commit_hash: "5cc1b62b9c866184193007a0f7ec3b2eb31460bf"
upstream_version: "0.8.0"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
  - loong64
  - riscv64

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []
