# tftp-proxy 构建配方 (Phase 1 自动生成)
repo: openfibernet/tftp-proxy
package: tftp-proxy
summary: "TFTP server that request files not found from an HTTP backend"
description: |
  TFTP server that request files not found from an HTTP backend
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/openfibernet/tftp-proxy
section: utils

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "v1.0.0"
latest_tag: "v1.0.0"
commit_hash: "f4d0a8adc4750032c4562f08fff295bb56e92d51"
upstream_version: "1.0.0"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
  - loong64
  - riscv64

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []
