# prometheus-snmp-exporter 构建配方 (Phase 1 自动生成)
repo: prometheus/snmp_exporter
package: prometheus-snmp-exporter
summary: "Prometheus exporter for SNMP-enabled devices"
description: |
  Prometheus exporter for SNMP-enabled devices
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/prometheus/snmp_exporter
section: net

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "v0.30.1"
latest_tag: "v0.30.1"
commit_hash: "48b810afbd9222b74439ad1cf7d0d7b7c0cf4290"
upstream_version: "0.30.1"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
  - loong64
  - riscv64

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []
