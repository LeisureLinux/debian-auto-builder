# golang-docker-credential-helpers 构建配方 (Phase 1 自动生成)
repo: docker/docker-credential-helpers
package: golang-docker-credential-helpers
summary: "native stores to safeguard Docker credentials"
description: |
  native stores to safeguard Docker credentials
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/docker/docker-credential-helpers
section: golang

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "v0.9.8"
latest_tag: "v0.9.8"
commit_hash: "4f6bc8ab27f64fbc65f810814448c756e958ab65"
upstream_version: "0.9.8"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
  - loong64
  - riscv64

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []
