# terraform-switcher 构建配方 (Phase 1 自动生成)
repo: warrensbox/terraform-switcher
package: terraform-switcher
summary: "switch between different versions of terraform (CLI tool)"
description: |
  switch between different versions of terraform (CLI tool)
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/warrensbox/terraform-switcher
section: utils

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "v1.19.0"
latest_tag: "v1.19.0"
commit_hash: "f6db709080818727968a226474c5d461e740fffd"
upstream_version: "1.19.0"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
  - loong64
  - riscv64

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []
