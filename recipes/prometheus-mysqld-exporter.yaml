# prometheus-mysqld-exporter 构建配方 (Phase 1 自动生成)
repo: prometheus/mysqld_exporter
package: prometheus-mysqld-exporter
summary: "Prometheus exporter for MySQL server"
description: |
  Prometheus exporter for MySQL server
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/prometheus/mysqld_exporter
section: net

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "v0.19.0"
latest_tag: "v0.19.0"
commit_hash: "464bc5de74325c3ba85b237561e4b6c0bc3ca6d7"
upstream_version: "0.19.0"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
  - loong64
  - riscv64

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []
