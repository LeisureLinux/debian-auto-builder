# prometheus-hacluster-exporter 构建配方 (Phase 1 自动生成)
repo: ClusterLabs/ha_cluster_exporter
package: prometheus-hacluster-exporter
summary: "Prometheus exporter for HA cluster services"
description: |
  Prometheus exporter for HA cluster services
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/ClusterLabs/ha_cluster_exporter
section: net

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "1.4.2"
latest_tag: "1.4.2"
commit_hash: "a50025a652b8f6854d49777f81cd0f2e015cfbdf"
upstream_version: "1.4.2"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
  - loong64
  - riscv64

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []
